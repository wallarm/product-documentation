```markdown
!!! info "Önkoşullar"
    Varsayılmaktadır ki
    
    *   [Docker Community Edition](https://docs.docker.com/install/) ve [`docker-compose`](https://docs.docker.com/compose/install/) zaten `10.0.30.30` Docker hostunda kurulmuş durumdadır.
    *   `node.example.local` filtre düğümü zaten dağıtılmış, yapılandırılmış, ileri yapılandırma için uygun (örneğin, SSH protokolü via) ve çalışır durumdadır.
```