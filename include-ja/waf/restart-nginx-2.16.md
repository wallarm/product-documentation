					=== "Debian"
    ```bash
    sudo systemctl nginxを再起動します
    ```
=== "Ubuntu"
    ```bash
    sudo service nginxを再起動します
    ```
=== "CentOSまたはAmazon Linux 2.0.2021x以下"
    ```bash
    sudo systemctl nginxを再起動します
    ```